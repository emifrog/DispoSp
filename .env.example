# Projet Supabase dédié. Ne jamais pointer vers une base déjà en service.
# Ces deux valeurs partent dans le paquet livré au navigateur : c’est prévu, la
# clé publiable ne donne accès qu’à ce que les policies RLS autorisent. Le projet
# n’a besoin d’aucune clé secrète, ni ici ni côté serveur.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# "connected" : l’utilisateur s’authentifie, les écrans lisent et écrivent la base.
# Toute autre valeur, ou rien : la démonstration locale, sans compte.
#
# Laisser "demo" en local. C’est le mode que `pnpm test:e2e` attend, et il évite
# que la présence de coordonnées Supabase place la démonstration derrière un
# écran de connexion. Pour une session connectée, sans toucher à ce fichier :
#
#   NEXT_PUBLIC_DISPOSP_MODE=connected pnpm dev
#
# Un déploiement, lui, doit porter "connected".
NEXT_PUBLIC_DISPOSP_MODE=demo

# --- Déploiement -------------------------------------------------------------
#
# Ce fichier n’est pas versionné et n’atteint jamais l’hébergeur : les trois
# variables ci-dessus doivent être déclarées dans le projet d’hébergement.
# NEXT_PUBLIC_* est figé dans le paquet au moment de la construction, donc une
# valeur modifiée ne prend effet qu’au redéploiement suivant.
#
# Côté Supabase, Authentication → URL Configuration doit lister l’adresse
# publique dans Site URL et Redirect URLs. Sans elle, le lien de confirmation
# d’adresse envoyé à un agent invité pointe vers localhost, et le rattachement
# de la migration 0004 ne se déclenche jamais.
